export enum SplitChoices {
	Split = 'split',
	Persons = 'persons',
	SplitWithPersons = 'split-with-persons',
}

export enum TaxOptions {
	PreTax = 'Pre-Tax',
	PostTax = 'Post-Tax',
}

export enum CostSymbols {
	Percent = 'percent',
	Currency = 'currency',
}
